<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Packlisten – Jörg Sawatzki</title>
    <meta name="description" content="Alle Packlisten für Wanderungen und Radreisen mit detaillierten Produktempfehlungen und Affiliate-Links.">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 min-h-screen py-12 px-4">
    
    <div class="max-w-6xl mx-auto">
        
        <!-- Back Link -->
        <a href="https://joergsawatzki.de" class="inline-flex items-center gap-2 text-gray-400 hover:text-orange-500 transition-colors mb-8">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" d="M10 19l-7-7m0 0l7-7m-7 7h18"/>
            </svg>
            Zur Startseite
        </a>
        
        <!-- Header -->
        <div class="text-center mb-12">
            <h1 class="text-5xl md:text-6xl font-bold text-white mb-4">📦 Packlisten</h1>
            <p class="text-xl text-gray-400 max-w-2xl mx-auto">
                Meine erprobten Packlisten für Wanderungen und Radreisen. Alle Produkte mit Affiliate-Links zu Amazon, Bergfreunde und weiteren Partnern.
            </p>
        </div>
        
        <!-- Packlisten Grid -->
        <div class="grid md:grid-cols-2 gap-8">
            
            {% for packliste in packlisten %}
            <a href="/{{ packliste.slug }}.html" class="group block bg-gray-800 rounded-2xl overflow-hidden hover:ring-2 hover:ring-orange-500 transition-all">
                <div class="relative h-64 overflow-hidden">
                    {% if packliste.hero_bild %}
                    <img src="{{ packliste.hero_bild }}" alt="{{ packliste.name }}" class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-300">
                    {% else %}
                    <div class="w-full h-full bg-gradient-to-br from-gray-800 to-gray-900"></div>
                    {% endif %}
                    <div class="absolute inset-0 bg-gradient-to-t from-gray-900 via-gray-900/60 to-transparent"></div>
                    <div class="absolute bottom-0 left-0 right-0 p-6">
                        <div class="flex items-center gap-3 mb-2">
                            <span class="text-4xl">{{ packliste.icon }}</span>
                            <h2 class="text-2xl font-bold text-white">{{ packliste.name }}</h2>
                        </div>
                        <p class="text-gray-300 text-sm line-clamp-2">{{ packliste.beschreibung }}</p>
                    </div>
                </div>
            </a>
            {% endfor %}
            
        </div>
        
        <!-- Info Box -->
        <div class="mt-12 bg-gray-800 border border-gray-700 rounded-xl p-6">
            <h3 class="text-lg font-bold text-white mb-2">💡 Über die Affiliate-Links</h3>
            <p class="text-gray-400 text-sm">
                Diese Seiten enthalten Affiliate-Links zu Amazon, Bergfreunde und weiteren Partnern. 
                Wenn du über diese Links kaufst, erhalte ich eine kleine Provision, ohne dass dir Mehrkosten entstehen. 
                So unterstützt du meine Arbeit an den YouTube-Videos. Mehr Details findest du im 
                <a href="606" class="text-orange-500 hover:text-orange-400 underline">Impressum</a>.
            </p>
        </div>
        
        <!-- Footer -->
        <p class="text-center text-gray-500 text-sm mt-12">
            © 2026 Jörg Sawatzki · 
            <a href="606" class="hover:text-gray-400 transition-colors">Impressum</a>
        </p>
        
    </div>
    
</body>
</html>